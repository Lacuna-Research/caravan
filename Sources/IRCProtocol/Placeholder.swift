/// Placeholder so the target has a source file before it is implemented.
///
/// Delete this file when IRCProtocol is built. It exists only so CI can prove the
/// target compiles, links, and is reachable from its test target.
public enum IRCProtocolPlaceholder {
    public static let isPlaceholder = true
}
